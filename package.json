{
  "name": "moneykeeper",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "npm run icons && vite build && cp dist/index.html dist/404.html && node scripts/build-sw.mjs",
    "preview": "vite preview",
    "test": "node --test test/*.test.js",
    "seed": "node scripts/seed-users.mjs",
    "set-password": "node scripts/set-password.mjs",
    "icons": "node scripts/build-icons.mjs"
  },
  "dependencies": {
    "firebase": "^11.1.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "sharp": "^0.35.4",
    "vite": "^6.0.5"
  }
}
